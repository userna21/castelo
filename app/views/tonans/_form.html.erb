<%= form_for(tonan) do |f| %>
  <% if tonan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tonan.errors.count, "error") %> prohibited this tonan from being saved:</h2>

      <ul>
      <% tonan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :sinop %>
    <%= f.text_area :sinop %>
  </div>

  <div class="field">
    <%= f.label :small %>
    <%= f.text_area :small %>
  </div>

  <div class="field">
    <%= f.label :medium %>
    <%= f.text_area :medium %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
